{
  "name": "root",
  "private": true,
  "scripts": {
    "start": "lerna run start --parallel",
    "gp": "git add . && git commit -m 'push it' && git push"
  },
  "devDependencies": {
    "lerna": "^3.22.1"
  }
}
